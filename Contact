<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Contact Us – CalcHub Pro</title>
<meta name="description" content="Contact CalcHub Pro. Send us your feedback, suggestions, bug reports, or any questions about our free online calculator tools.">
<link rel="canonical" href="https://calchubpro.in/contact.html">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,700;12..96,800&family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{margin:0;padding:0;box-sizing:border-box}
body{font-family:'DM Sans',sans-serif;background:#07090f;color:#f0f2f8;line-height:1.7;overflow-x:hidden}
a{color:#38bdf8;text-decoration:underline}
:root{
  --red:#e0293e;--red-glow:rgba(224,41,62,.3);--red-dim:rgba(224,41,62,.1);
  --border:rgba(255,255,255,.07);--border2:rgba(255,255,255,.13);
  --muted:rgba(240,242,248,.55);--glass:rgba(255,255,255,.03);--glass2:rgba(255,255,255,.06);
  --lbg:#f4f5fb;--lcard:#ffffff;--lborder:#e2e5f2;--lprimary:#080c26;--lacce:#e0293e;--lmuted:#6672a8;--llight:#eceef9;
  --ok:#22c55e;--danger:#ef4444;
  --ff-head:'Bricolage Grotesque',sans-serif;
}
.nav{position:sticky;top:0;z-index:900;height:60px;display:flex;align-items:center;padding:0 max(20px,5vw);background:rgba(7,9,15,.92);backdrop-filter:blur(20px);border-bottom:1px solid var(--border);gap:14px}
.nav-logo{display:flex;align-items:center;gap:10px;font-family:var(--ff-head);font-size:1.1rem;font-weight:800;text-decoration:none;color:#f0f2f8}
.logo-mark{width:32px;height:32px;background:linear-gradient(135deg,var(--red),#9e0010);border-radius:9px;display:grid;place-items:center;font-size:1rem;flex-shrink:0}
.hi{color:var(--red)}
.nav-back{margin-left:auto;background:var(--glass);border:1px solid var(--border2);color:#f0f2f8;padding:7px 16px;border-radius:50px;font-size:.82rem;font-weight:600;text-decoration:none;transition:all .2s}
.nav-back:hover{background:var(--red);border-color:var(--red);color:#fff}
/* HERO */
.page-hero{text-align:center;padding:60px max(20px,5vw) 40px;position:relative;overflow:hidden}
.page-hero::before{content:'';position:absolute;top:-30%;left:50%;transform:translateX(-50%);width:500px;height:300px;background:radial-gradient(ellipse,rgba(224,41,62,.09),transparent 68%);pointer-events:none}
.page-chip{display:inline-flex;align-items:center;gap:7px;background:var(--red-dim);border:1px solid rgba(224,41,62,.22);color:#ff8094;padding:4px 13px;border-radius:50px;font-size:.7rem;font-weight:700;letter-spacing:1.2px;text-transform:uppercase;margin-bottom:16px}
.page-hero h1{font-family:var(--ff-head);font-size:clamp(2rem,5vw,3rem);font-weight:800;letter-spacing:-.8px;margin-bottom:13px}
.page-hero h1 em{color:var(--red);font-style:normal}
.page-hero p{color:var(--muted);font-size:.95rem;max-width:480px;margin:0 auto}
/* FORM SECTION */
.form-section{background:var(--lbg);padding:52px max(20px,5vw) 72px;border-top:3px solid var(--red)}
.form-wrap{max-width:620px;margin:0 auto;background:var(--lcard);border-radius:20px;padding:36px;box-shadow:0 12px 52px rgba(8,12,38,.12)}
.form-wrap h2{font-family:var(--ff-head);font-size:1.35rem;font-weight:800;color:var(--lprimary);margin-bottom:6px}
.form-wrap p.sub{color:var(--lmuted);font-size:.86rem;margin-bottom:26px}
.field{margin-bottom:18px}
.field label{display:block;font-size:.875rem;font-weight:600;color:var(--lprimary);margin-bottom:7px}
.field input,.field select,.field textarea{width:100%;padding:12px 15px;border:2px solid var(--lborder);border-radius:10px;font-size:.93rem;font-family:inherit;color:var(--lprimary);background:var(--llight);outline:none;transition:border-color .2s,box-shadow .2s;resize:vertical}
.field input:focus,.field select:focus,.field textarea:focus{border-color:var(--lacce);box-shadow:0 0 0 4px rgba(224,41,62,.08);background:#fff}
.field textarea{min-height:130px}
.ferr{display:none;color:var(--danger);font-size:.74rem;margin-top:4px;font-weight:500}
.ferr.show{display:block}
.submit-btn{width:100%;padding:14px;background:var(--lacce);color:#fff;border:none;border-radius:10px;font-size:.97rem;font-weight:700;cursor:pointer;font-family:inherit;transition:all .22s;position:relative;overflow:hidden;margin-top:4px}
.submit-btn:hover{background:#c9182c;transform:translateY(-2px);box-shadow:0 7px 22px rgba(224,41,62,.35)}
.submit-btn:active{transform:scale(.98)}
.success-msg{display:none;background:rgba(34,197,94,.08);border:1px solid rgba(34,197,94,.25);border-left:3px solid var(--ok);border-radius:10px;padding:16px 20px;margin-top:18px;color:rgba(8,12,38,.8);font-size:.88rem}
.success-msg.show{display:block}
/* INFO CARDS */
.info-grid{max-width:900px;margin:0 auto;padding:60px max(20px,5vw);display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.info-card{background:var(--glass);border:1px solid var(--border);border-radius:14px;padding:24px;text-align:center;transition:all .28s}
.info-card:hover{background:var(--glass2);border-color:var(--border2);transform:translateY(-4px)}
.info-ico{font-size:1.8rem;margin-bottom:12px}
.info-card h3{font-family:var(--ff-head);font-size:.95rem;font-weight:700;margin-bottom:7px}
.info-card p{color:var(--muted);font-size:.82rem;line-height:1.65;margin:0}
@media(max-width:640px){.info-grid{grid-template-columns:1fr}.form-wrap{padding:24px 18px}}
footer.mini{text-align:center;padding:28px 20px;border-top:1px solid var(--border);color:var(--muted);font-size:.78rem}
footer.mini a{color:var(--muted)}
</style>
</head>
<body>
<nav class="nav">
  <a href="index.html" class="nav-logo">
    <div class="logo-mark">🧮</div>
    <span>Calc<span class="hi">Hub</span> Pro</span>
  </a>
  <a href="index.html" class="nav-back">← Back to Home</a>
</nav>

<!-- HERO -->
<section class="page-hero">
  <div class="page-chip">💬 Support</div>
  <h1>Contact <em>Us</em></h1>
  <p>Have feedback, a bug report, or a suggestion? We would love to hear from you. Fill out the form below and we will get back to you shortly.</p>
</section>

<!-- INFO CARDS -->
<div class="info-grid">
  <div class="info-card">
    <div class="info-ico">🐛</div>
    <h3>Report a Bug</h3>
    <p>Found something not working correctly? Tell us the calculator name and what went wrong.</p>
  </div>
  <div class="info-card">
    <div class="info-ico">💡</div>
    <h3>Suggest a Feature</h3>
    <p>Want a new calculator or an improvement to an existing one? We are always open to ideas.</p>
  </div>
  <div class="info-card">
    <div class="info-ico">🤝</div>
    <h3>General Enquiry</h3>
    <p>Questions about our tools, privacy practices, or advertising? Reach out any time.</p>
  </div>
</div>

<!-- FORM -->
<section class="form-section">
  <div class="form-wrap">
    <h2>Send Us a Message</h2>
    <p class="sub">We typically respond within 1–2 business days.</p>

    <div class="field">
      <label for="c-name">Your Name *</label>
      <input type="text" id="c-name" placeholder="e.g. Rahul Sharma" maxlength="100">
      <div class="ferr" id="err-name">Please enter your name.</div>
    </div>
    <div class="field">
      <label for="c-email">Email Address *</label>
      <input type="email" id="c-email" placeholder="e.g. rahul@email.com">
      <div class="ferr" id="err-email">Please enter a valid email address.</div>
    </div>
    <div class="field">
      <label for="c-subject">Subject *</label>
      <select id="c-subject">
        <option value="">— Select a subject —</option>
        <option value="bug">Bug Report</option>
        <option value="feature">Feature Request</option>
        <option value="content">Content Correction</option>
        <option value="privacy">Privacy / Data Query</option>
        <option value="ads">Advertising Enquiry</option>
        <option value="other">Other</option>
      </select>
      <div class="ferr" id="err-subject">Please select a subject.</div>
    </div>
    <div class="field">
      <label for="c-msg">Message *</label>
      <textarea id="c-msg" placeholder="Describe your issue, suggestion, or question in detail..."></textarea>
      <div class="ferr" id="err-msg">Please enter your message (at least 20 characters).</div>
    </div>
    <button class="submit-btn" onclick="submitForm()">Send Message →</button>
    <div class="success-msg" id="success-msg">
      ✅ <strong>Thank you!</strong> Your message has been received. We will get back to you within 1–2 business days.
    </div>
  </div>
</section>

<footer class="mini">
  <p>© 2025 CalcHub Pro · <a href="privacy-policy.html">Privacy Policy</a> · <a href="terms.html">Terms of Use</a> · <a href="disclaimer.html">Disclaimer</a> · <a href="contact.html">Contact</a></p>
</footer>

<script>
function submitForm(){
  let ok=true;
  const name=document.getElementById('c-name').value.trim();
  const email=document.getElementById('c-email').value.trim();
  const subject=document.getElementById('c-subject').value;
  const msg=document.getElementById('c-msg').value.trim();

  const setErr=(id,bad)=>{
    const el=document.getElementById(id);
    if(bad){el.classList.add('show');ok=false;}
    else el.classList.remove('show');
  };

  setErr('err-name',!name);
  setErr('err-email',!email||!email.includes('@')||!email.includes('.'));
  setErr('err-subject',!subject);
  setErr('err-msg',msg.length<20);

  if(!ok)return;

  // Show success message (form is static — replace with backend/formspree if needed)
  document.getElementById('success-msg').classList.add('show');
  document.getElementById('c-name').value='';
  document.getElementById('c-email').value='';
  document.getElementById('c-subject').value='';
  document.getElementById('c-msg').value='';
  document.getElementById('success-msg').scrollIntoView({behavior:'smooth',block:'center'});
}
</script>
</body>
</html>
